import{_ as a,C as c,o as s,c as d,a3 as o,b as i,w as t,a as r,E as l,a4 as p}from"./chunks/framework.4rHeyzPv.js";const v=JSON.parse(`{"title":"证据阅读说明","description":"","frontmatter":{"title":"证据阅读说明","route":"/evidence-guide","stage":"reference","scope":"全站证据标签说明，想知道某条结论有多可信时回来查","anchors":[{"path":"learn/scripts/check-anchors.ts","line":40,"expect":"KINDS = new Set(['source', 'test', 'runtime', 'design', 'cross', 'unverified'])","kind":"source"},{"path":"learn/scripts/check-anchors.ts","line":55,"expect":"未验证判断: 'unverified'","kind":"source"},{"path":"learn/scripts/check-anchors.ts","line":37,"expect":"DRIFT_WINDOW = 5","kind":"source"},{"path":"learn/scripts/check-anchors.ts","line":191,"expect":"function checkAnchor","kind":"source"},{"path":"learn/scripts/check-anchors.ts","line":207,"expect":"kind === 'unverified'","kind":"source"},{"path":"learn/scripts/check-anchors.ts","line":229,"expect":"DRIFT_WINDOW","kind":"source"},{"path":"learn/scripts/check-anchors.ts","line":322,"expect":"totals.stale > 0","kind":"source"},{"path":"learn/.vitepress/config.ts","line":287,"expect":"ignoreDeadLinks: true","kind":"source"},{"path":"learn/.vitepress/config.ts","line":68,"expect":"interface LearnMeta","kind":"source"},{"path":"learn/.vitepress/config.ts","line":16,"expect":"const siteRoot","kind":"source"},{"path":"learn/.vitepress/theme/PageMeta.vue","line":84,"expect":"const stale","kind":"source"},{"path":"learn/.vitepress/theme/PageMeta.vue","line":191,"expect":"本站是非官方的中文源码学习材料","kind":"source"},{"path":"AGENTS.md","line":3,"expect":"everything is a plugin","kind":"design"},{"path":"docs/architecture.md","line":5,"expect":"Read this before changing anything","kind":"design"}]},"headers":[],"relativePath":"evidence-guide.md","filePath":"evidence-guide.md"}`),h={name:"evidence-guide.md"};function u(k,e,g,m,b,E){const n=c("Mermaid");return s(),d("div",null,[e[1]||(e[1]=o('<h1 id="证据阅读说明" tabindex="-1">证据阅读说明 <a class="header-anchor" href="#证据阅读说明" aria-label="Permalink to &quot;证据阅读说明&quot;">​</a></h1><h2 id="这页解决什么问题" tabindex="-1">这页解决什么问题 <a class="header-anchor" href="#这页解决什么问题" aria-label="Permalink to &quot;这页解决什么问题&quot;">​</a></h2><p>本站的每条关键结论后面都带着一个证据标签，比如【源码事实】、【设计记录】。这页告诉你每种标签代表什么、可信度有多高、你自己怎么复核，以及本站的锚点校验系统是怎么工作的。</p><p>读完这一页，你能独立判断一条结论的可靠性，而不是被动地接受。本站是非官方的中文源码学习材料，不是 DeepSeek Harness 的官方文档；结论以源码为准，证据标签和锚点只是帮你快速定位到源码对应位置的工具。</p><h2 id="六种证据标签" tabindex="-1">六种证据标签 <a class="header-anchor" href="#六种证据标签" aria-label="Permalink to &quot;六种证据标签&quot;">​</a></h2><p>每种证据标签对应 <code>check-anchors.ts</code> 中 <code>KINDS</code> 集合的一个值（<code>learn/scripts/check-anchors.ts:40</code>）。标签出现在正文中括号内，如【源码事实】。<code>kind</code> 字段在页面 frontmatter 的 <code>anchors</code> 数组里声明，由 <code>check-anchors.ts</code> 在每次运行时校验。</p><h3 id="_1-【源码事实】—-kind-source" tabindex="-1">1. 【源码事实】— <code>kind: source</code> <a class="header-anchor" href="#_1-【源码事实】—-kind-source" aria-label="Permalink to &quot;1. 【源码事实】— `kind: source`&quot;">​</a></h3><p><strong>含义</strong>：结论直接来自源码文件中的一行或多行代码。是最强证据，因为源码是仓库里唯一不依赖外部解释的权威信息来源。</p><p><strong>可信度</strong>：最高。只要该文件在 <code>check-anchors</code> 校验时对应的行仍然包含期望内容，结论就是可靠的。如果该行被改过，校验会标记为&quot;漂移&quot;或&quot;失效&quot;。</p><p><strong>你自己怎么复核</strong>：打开页面底部&quot;本页元信息&quot;区域，找到对应锚点的 <code>path:line</code> 链接，点击即可跳转到 GitHub 上该 commit 的对应行。也可以在你的本地仓库中打开同一文件同一行。</p><p><strong>例子</strong>：</p><blockquote><p><code>SESSION_FORMAT_VERSION</code> 当前值为 <code>0</code>（<code>packages/core/session/src/types.ts:56</code>）。【源码事实】</p></blockquote><p>上面这条结论的 frontmatter 锚点声明为：</p><div class="language-yaml vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">packages/core/session/src/types.ts</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  line</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">56</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  expect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SESSION_FORMAT_VERSION&quot;</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">source</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><h3 id="_2-【测试行为】—-kind-test" tabindex="-1">2. 【测试行为】— <code>kind: test</code> <a class="header-anchor" href="#_2-【测试行为】—-kind-test" aria-label="Permalink to &quot;2. 【测试行为】— `kind: test`&quot;">​</a></h3><p><strong>含义</strong>：结论来自测试文件（<code>.spec.ts</code>）中描述的行为。测试文件描述的是&quot;代码应该怎么做&quot;，而不是&quot;代码实际怎么做&quot;——但测试本身就是可执行的行为规范，比文档更可靠。</p><p><strong>可信度</strong>：高，但低于【源码事实】。测试可能因为 mock 覆盖不全、边界情况遗漏、或测试本身有 bug 而给出不完整的图景。<code>check-anchors</code> 对测试锚点的校验逻辑和【源码事实】完全一致：在目标行里找期望子串。</p><p><strong>你自己怎么复核</strong>：和【源码事实】一样，点击锚点链接跳转到测试文件对应行，用 <code>pnpm run test</code> 跑一遍确认测试仍然通过。</p><p><strong>例子</strong>：</p><blockquote><p><code>session.deriveMessages()</code> 在测试中被直接调用，用于确认从事件到模型消息的投影逻辑（<code>packages/core/session/tests/session.spec.ts:54</code>）。【测试行为】</p></blockquote><h3 id="_3-【运行验证】—-kind-runtime" tabindex="-1">3. 【运行验证】— <code>kind: runtime</code> <a class="header-anchor" href="#_3-【运行验证】—-kind-runtime" aria-label="Permalink to &quot;3. 【运行验证】— `kind: runtime`&quot;">​</a></h3><p><strong>含义</strong>：结论是实际运行代码后观察到的行为。不是从源码静态推导的，也不是从测试推出来的，而是真的跑了一遍 <code>pnpm dsh --profile headless &quot;...&quot;</code> 或 <code>pnpm run demo:acp</code> 后记录下来的。</p><p><strong>可信度</strong>：高，但受限于当时运行的版本、配置和环境。如果代码或其依赖更新了，这条结论可能不再成立，但 <code>check-anchors</code> 无法自动检测——因为运行验证锚点指向的仍然是源码中某行，而不是运行时的日志输出。</p><p><strong>你自己怎么复核</strong>：按本站相同的方式跑一遍命令。大多数运行验证锚点会注明具体命令。</p><p><strong>例子</strong>：</p><blockquote><p>运行 <code>pnpm dsh --profile headless &quot;echo hello&quot;</code> 后，session log 中第一个 <code>turn/start</code> 事件出现在第 3 条记录位置。【运行验证】</p></blockquote><h3 id="_4-【设计记录】—-kind-design" tabindex="-1">4. 【设计记录】— <code>kind: design</code> <a class="header-anchor" href="#_4-【设计记录】—-kind-design" aria-label="Permalink to &quot;4. 【设计记录】— `kind: design`&quot;">​</a></h3><p><strong>含义</strong>：结论来自仓库中的设计文档，包括 <code>AGENTS.md</code>、<code>docs/architecture.md</code>、<code>docs/glossary.md</code>、<code>docs/defensive-patterns.md</code> 等。这些文档是项目维护者写的，描述的是&quot;项目应该是什么样&quot;——设计意图，而非实现细节。</p><p><strong>可信度</strong>：中高。设计文档是项目维护者写的第一手材料，但可能落后于实现。AGENTS.md 作为仓库规范文件，可信度最高；<code>docs/</code> 下的其他文档次之。<code>check-anchors</code> 对设计文档锚点的校验逻辑和源码一致。</p><p><strong>你自己怎么复核</strong>：点击锚点链接查看文档原文，再对照源码看实现是否和文档描述一致。如果文档说&quot;插件注册返回 disposer&quot;，但源码中某处没有返回 disposer，文档就是对的而实现有 bug，或者文档过时了。</p><p><strong>例子</strong>：</p><blockquote><p>一切皆插件（everything is a plugin，<code>AGENTS.md:3</code>）。【设计记录】</p></blockquote><h3 id="_5-【跨源码分析】—-kind-cross" tabindex="-1">5. 【跨源码分析】— <code>kind: cross</code> <a class="header-anchor" href="#_5-【跨源码分析】—-kind-cross" aria-label="Permalink to &quot;5. 【跨源码分析】— `kind: cross`&quot;">​</a></h3><p><strong>含义</strong>：结论不是从单一文件或单一文档得出的，而是跨多个文件、多个 package 分析后得出的推断。这种推断对理解架构很有价值，但它不是任何一个文件的直接陈述。</p><p><strong>可信度</strong>：中。跨文件分析的结论依赖分析者的理解，可能有遗漏或误解。<code>check-anchors</code> 对跨源码分析锚点的校验和单文件锚点一致——它只验证每个锚点指向的单个文件行是否仍然包含期望内容，不验证跨文件的关系是否仍然成立。</p><p><strong>你自己怎么复核</strong>：按锚点逐文件查看，再自己对照着看。如果三条锚点分别指向 <code>agent.ts</code>、<code>session.ts</code>、<code>types.ts</code>，你需要自己理解它们之间的关系，本站只帮你把文件位置和行号标记出来。</p><p><strong>例子</strong>：</p><blockquote><p><code>agent-loop</code> 发送 <code>turn/start</code> 事件（<code>packages/core/agent-loop/src/agent.ts:255</code>）后，<code>Session.append</code> 将其写入只追加日志（<code>packages/core/session/src/index.ts:604</code>），事件类型由 <code>SessionEventMap</code> 约束（<code>packages/core/session/src/types.ts:236</code>）。这三处构成了一条完整的&quot;事件发出 → 日志写入 → 类型约束&quot;链路。【跨源码分析】</p></blockquote><h3 id="_6-【未验证判断】—-kind-unverified" tabindex="-1">6. 【未验证判断】— <code>kind: unverified</code> <a class="header-anchor" href="#_6-【未验证判断】—-kind-unverified" aria-label="Permalink to &quot;6. 【未验证判断】— `kind: unverified`&quot;">​</a></h3><p><strong>含义</strong>：结论是本站作者基于阅读经验做出的判断，但尚未找到精确的源码锚点来证实。这类判断可能对（比如&quot;这个包似乎没有被任何地方 import&quot;），也可能错（因为有的 import 路径不直观）。</p><p><strong>可信度</strong>：低。在看到反证之前可以当作参考，但不应作为决策依据。<code>check-anchors</code> 对 <code>kind: unverified</code> 做特殊处理：如果 <code>path</code> 和 <code>line</code> 都缺失，直接跳过不校验（<code>learn/scripts/check-anchors.ts:207</code>）；如果提供了 <code>path</code> 和 <code>line</code>，仍然正常校验，但标记为 <code>skipped</code> 而非 <code>stale</code>。</p><p><strong>你自己怎么复核</strong>：逐条质疑。对于&quot;某包似乎没有被引用&quot;的判断，用 <code>grep</code> 全仓库搜索确认；对于&quot;某设计可能是为了&quot;的判断，直接看源码验证。</p><p><strong>例子</strong>：</p><blockquote><p><code>packages/e2b/</code> 的 sandbox 适配器似乎没有被任何其他 package 直接依赖，它可能是一个独立的 POC 模块。【未验证判断】</p></blockquote><h2 id="证据标签的可信度排序" tabindex="-1">证据标签的可信度排序 <a class="header-anchor" href="#证据标签的可信度排序" aria-label="Permalink to &quot;证据标签的可信度排序&quot;">​</a></h2><table tabindex="0"><thead><tr><th>可信度</th><th>标签</th><th><code>kind</code> 值</th><th>自动校验</th></tr></thead><tbody><tr><td>最高</td><td>源码事实</td><td><code>source</code></td><td>是</td></tr><tr><td>高</td><td>测试行为</td><td><code>test</code></td><td>是</td></tr><tr><td>高</td><td>运行验证</td><td><code>runtime</code></td><td>是</td></tr><tr><td>中高</td><td>设计记录</td><td><code>design</code></td><td>是</td></tr><tr><td>中</td><td>跨源码分析</td><td><code>cross</code></td><td>是（仅校验单点）</td></tr><tr><td>低</td><td>未验证判断</td><td><code>unverified</code></td><td>可选（无 path/line 时跳过）</td></tr></tbody></table><h2 id="锚点校验系统" tabindex="-1">锚点校验系统 <a class="header-anchor" href="#锚点校验系统" aria-label="Permalink to &quot;锚点校验系统&quot;">​</a></h2><p>本站的每条证据标签都对应一个 frontmatter 锚点，由 <code>learn/scripts/check-anchors.ts</code> 脚本在每次运行时校验。</p>',48)),(s(),i(p,null,{default:t(()=>[l(n,{id:"mermaid-255",class:"mermaid",graph:"flowchart%20TD%0A%20%20A%5B%22%E8%AF%BB%E5%8F%96%E9%A1%B5%E9%9D%A2%20frontmatter%3Cbr%2F%3E%E5%8F%96%E5%87%BA%20anchors%22%5D%20--%3E%20B%7B%22kind%20%E5%90%88%E6%B3%95%EF%BC%9F%22%7D%0A%20%20B%20--%20%E5%90%A6%20--%3E%20E%5B%22%E6%A0%BC%E5%BC%8F%E9%94%99%E8%AF%AF%20%E2%86%92%20exit%201%22%5D%0A%20%20B%20--%20%E6%98%AF%20--%3E%20C%7B%22unverified%20%E4%B8%94%E6%97%A0%20path%2Fline%EF%BC%9F%22%7D%0A%20%20C%20--%20%E6%98%AF%20--%3E%20S%5B%22%E8%B7%B3%E8%BF%87%22%5D%0A%20%20C%20--%20%E5%90%A6%20--%3E%20D%7B%22%E7%AC%AC%20line%20%E8%A1%8C%E5%8C%85%E5%90%AB%20expect%EF%BC%9F%22%7D%0A%20%20D%20--%20%E6%98%AF%20--%3E%20OK%5B%22%E5%91%BD%E4%B8%AD%22%5D%0A%20%20D%20--%20%E5%90%A6%20--%3E%20W%7B%22%C2%B15%20%E8%A1%8C%E5%86%85%E6%89%BE%E5%88%B0%EF%BC%9F%22%7D%0A%20%20W%20--%20%E6%98%AF%20--%3E%20DR%5B%22%E6%BC%82%E7%A7%BB%20%E2%86%92%20%E8%AD%A6%E5%91%8A%EF%BC%8Cexit%200%22%5D%0A%20%20W%20--%20%E5%90%A6%20--%3E%20ST%5B%22%E5%A4%B1%E6%95%88%20%E2%86%92%20exit%201%22%5D%0A"})]),fallback:t(()=>[...e[0]||(e[0]=[r(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=o(`<p>图说明：这就是下面七条规则的流程形态。三个终态需要你行动——<strong>格式错误</strong>改 frontmatter 的 <code>kind</code>，<strong>漂移</strong>改行号，<strong>失效</strong>要回去核对源码是否真的变了。只有&quot;命中&quot;和&quot;跳过&quot;是无需处理的。</p><h3 id="校验逻辑" tabindex="-1">校验逻辑 <a class="header-anchor" href="#校验逻辑" aria-label="Permalink to &quot;校验逻辑&quot;">​</a></h3><p><code>check-anchors.ts</code> 的校验过程（<code>learn/scripts/check-anchors.ts:191</code>）：</p><ol><li><strong>读取每个页面的 frontmatter</strong>，提取 <code>anchors</code> 数组。</li><li><strong>对每条锚点</strong>，打开它声明的 <code>path</code> 文件，跳到第 <code>line</code> 行，检查该行是否包含 <code>expect</code> 子串（不是精确相等，而是 <code>includes</code>，所以代码格式化不会导致误报）。</li><li><strong>如果该行不包含期望内容</strong>，在前后各 5 行（<code>DRIFT_WINDOW = 5</code>，<code>learn/scripts/check-anchors.ts:37</code>）的窗口内搜索同一内容。</li><li><strong>窗口内找到了</strong> → 标记为&quot;行号漂移&quot;，警告但不报错（exit 0）。</li><li><strong>窗口内找不到</strong> → 标记为&quot;锚点失效&quot;，报错退出（exit 1，<code>learn/scripts/check-anchors.ts:322</code>）。</li><li><strong><code>kind: unverified</code> 且无 <code>path</code>/<code>line</code></strong> → 跳过不校验。</li><li><strong><code>kind</code> 不在六种合法值内</strong> → 标记为格式错误，报错退出。</li></ol><h3 id="校验结果" tabindex="-1">校验结果 <a class="header-anchor" href="#校验结果" aria-label="Permalink to &quot;校验结果&quot;">​</a></h3><p>校验结果写入 <code>learn/.vitepress/anchor-status.json</code>（<code>learn/.vitepress/config.ts:68</code> 中 <code>LearnMeta</code> 接口的 <code>anchors</code> 字段），包含：</p><ul><li><code>checkedAt</code>：校验时间（ISO 格式）</li><li><code>commit</code>：校验时仓库所在的 commit</li><li><code>total</code>：全站锚点总数</li><li><code>staleCount</code>：失效锚点数</li><li><code>driftedCount</code>：漂移锚点数</li><li><code>pages</code>：按页面路由分组的详细结果</li></ul><p>每个页面的 <code>PageStatus</code> 包含 <code>stale</code>（失效）、<code>drifted</code>（漂移）和 <code>invalid</code>（格式错误）三个数组，每个数组元素都是一条 <code>AnchorReport</code>，包含 <code>path</code>、<code>line</code>、<code>expect</code>、<code>kind</code>、<code>reason</code>（失败原因）等信息。</p><h3 id="页面底部展示" tabindex="-1">页面底部展示 <a class="header-anchor" href="#页面底部展示" aria-label="Permalink to &quot;页面底部展示&quot;">​</a></h3><p><code>learn/.vitepress/theme/PageMeta.vue</code> 在每个页面底部渲染锚点校验状态（<code>learn/.vitepress/theme/PageMeta.vue:84</code>）：</p><ul><li><strong>失效</strong>：红色警告框，列出所有失效锚点，提示&quot;结论可能过期&quot;。</li><li><strong>漂移</strong>：黄色提示框，列出所有漂移锚点，提示&quot;引用内容仍在，但行号已变化&quot;。</li><li><strong>全部命中</strong>：绿色文字，无警告框。</li></ul><p>页面底部还会显示当前源码 commit、页面生成时间、适用范围（<code>scope</code> 字段），以及本站的免责声明（<code>learn/.vitepress/theme/PageMeta.vue:191</code>）。</p><h2 id="你自己怎么运行锚点校验" tabindex="-1">你自己怎么运行锚点校验 <a class="header-anchor" href="#你自己怎么运行锚点校验" aria-label="Permalink to &quot;你自己怎么运行锚点校验&quot;">​</a></h2><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 在仓库根目录下运行</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> learn:check</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 等价的直接调用</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tsx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> learn/scripts/check-anchors.ts</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>输出示例：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>check-anchors: 11 个页面，362 条锚点 → 命中 316，漂移 4，失效 38，格式错误 20，跳过 4（commit 47f9438）</span></span>
<span class="line"><span>  ✓ /  (index.md)  锚点 6：命中 6，漂移 0，失效 0，格式错误 0，跳过 0</span></span>
<span class="line"><span>  ✓ /glossary  (glossary.md)  锚点 77：命中 77，漂移 0，失效 0，格式错误 0，跳过 0</span></span>
<span class="line"><span>  ✗ /session-log  (session-log.md)  锚点 20：命中 8，漂移 1，失效 11，格式错误 20，跳过 0</span></span>
<span class="line"><span>      行号漂移：packages/session/session-persistence-sqlite/src/schema.ts:47 → 应为 49（期望包含 &quot;EventRow&quot;）</span></span>
<span class="line"><span>      锚点失效：packages/core/session/src/index.ts:417 — 第 417 行及 ±5 行窗口内均不含期望内容</span></span>
<span class="line"><span>      格式错误：packages/core/session/src/index.ts:417 — kind 取值非法：&quot;Session 类声明&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>上面这段是本站在 commit <code>47f9438</code> 上的<strong>真实输出</strong>：本站的 7 个参考与路线页已全部命中，剩下的失效与格式错误集中在尚在改写中的几个子系统页上。<strong>注意那条格式错误：<code>kind</code> 写成中文描述（如“Session 类声明”）是最常见的错法</strong>——<code>kind</code> 只能是六个枚举值之一（或它们对应的中文标签，映射表在 <code>learn/scripts/check-anchors.ts:55</code> 附近），不是给锚点写注释的地方。</p><ul><li><code>✓</code> 表示该页全部锚点通过</li><li><code>!</code> 表示该页有行号漂移</li><li><code>✗</code> 表示该页有失效锚点或格式错误</li></ul><p><strong>退出码</strong>：</p><ul><li><code>0</code>：全部通过或仅有漂移（警告但不阻止 CI）</li><li><code>1</code>：有失效锚点或格式错误</li></ul><h2 id="证据标签的局限" tabindex="-1">证据标签的局限 <a class="header-anchor" href="#证据标签的局限" aria-label="Permalink to &quot;证据标签的局限&quot;">​</a></h2><p>本站的证据标签和锚点校验系统有几个已知局限，你在阅读时应该心里有数：</p><ol><li><strong>锚点校验只检查单行子串匹配</strong>。一条结论说&quot;A 和 B 共同构成 C&quot;，锚点校验只验证 A 那行还在、B 那行还在，不验证它们之间的关系是否仍然成立。这是【跨源码分析】天然的风险。</li><li><strong>锚点校验不检查语义变化</strong>。某行代码从 <code>const x = 1</code> 变成 <code>const x = 2</code>，锚点校验不会报错——因为 <code>expect: &quot;const x&quot;</code> 仍然匹配。本站作者在写 <code>expect</code> 时会尽量选择语义关键的标识符（如函数名、常量名），但无法完全消除这种风险。</li><li><strong>未验证判断没有锚点校验</strong>。本站的所有【未验证判断】汇总在 <a href="/deepseek-harness-learn/coverage">/coverage</a> 页面中，你可以逐条复核。</li><li><strong>锚点校验依赖 <code>git</code> 和文件系统</strong>。它在提交前运行（<code>pnpm run learn:check</code>），但 VitePress 构建时只是读取上一次的校验结果（<code>learn/.vitepress/anchor-status.json</code>），不会重新跑校验。所以在两次校验之间，如果代码改了但没重新校验，页面底部的状态可能滞后。</li><li><strong>VitePress 构建允许死链</strong>。config 中设置了 <code>ignoreDeadLinks: true</code>（<code>learn/.vitepress/config.ts:287</code>），因为页面是逐步添加的，一个尚未写好的页面不应导致构建失败。<code>check-anchors</code> 和 <code>check-mermaid</code> 才是真正的质量门。</li></ol><h2 id="下一步" tabindex="-1">下一步 <a class="header-anchor" href="#下一步" aria-label="Permalink to &quot;下一步&quot;">​</a></h2><ul><li>想知道本站实际读了哪些源码、哪些判断尚未验证 → <a href="/deepseek-harness-learn/coverage">/coverage</a></li><li>想了解本站的术语定义 → <a href="/deepseek-harness-learn/glossary">/glossary</a></li><li>回到阅读路线 → <a href="/deepseek-harness-learn/">/</a></li></ul>`,25))])}const f=a(h,[["render",u]]);export{v as __pageData,f as default};
