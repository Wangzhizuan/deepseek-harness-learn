import{_ as l,C as p,o as c,c as g,a3 as d,b as n,w as t,a as o,E as a,a4 as i,j as s}from"./chunks/framework.DACwFHq9.js";const S=JSON.parse(`{"title":"一页理解 Harness","description":"","frontmatter":{"title":"一页理解 Harness","route":"/harness-explained","stage":"start","scope":"用一页篇幅讲清 DeepSeek Harness 是什么、由哪几块组成、一个请求怎么走完；不深入任何单个子系统的实现细节。","anchors":[{"path":"AGENTS.md","line":3,"expect":"everything is a plugin","kind":"design"},{"path":"AGENTS.md","line":7,"expect":"no compatibility promise","kind":"design"},{"path":"AGENTS.md","line":109,"expect":"Service Definition / Service Provider / Consumer","kind":"design"},{"path":"AGENTS.md","line":111,"expect":"request/spec split is the template","kind":"design"},{"path":"docs/architecture.md","line":11,"expect":"Cordis","kind":"design"},{"path":"docs/architecture.md","line":13,"expect":"no privileged core to patch","kind":"design"},{"path":"docs/architecture.md","line":17,"expect":"plugin tree composed at boot","kind":"design"},{"path":"docs/architecture.md","line":19,"expect":"named composition stored in the Harness home","kind":"design"},{"path":"docs/architecture.md","line":21,"expect":"distribution format for Cordis config rows","kind":"design"},{"path":"docs/architecture.md","line":57,"expect":"durable facts appended to the log","kind":"design"},{"path":"docs/architecture.md","line":58,"expect":"carry a live \`Agent\`","kind":"design"},{"path":"docs/architecture.md","line":65,"expect":"one model request plus the tools it calls","kind":"design"},{"path":"docs/architecture.md","line":84,"expect":"waterfalls, whose listeners must call","kind":"design"},{"path":"docs/architecture.md","line":96,"expect":"Model-visible means logged","kind":"design"},{"path":"docs/architecture.md","line":100,"expect":"swappable capability with three roles","kind":"design"},{"path":"docs/architecture.md","line":102,"expect":"moves Bash, PTY, and LSP with them","kind":"design"},{"path":"website/AGENTS.md","line":7,"expect":"owns only VitePress configuration","kind":"design"},{"path":"website/AGENTS.md","line":11,"expect":"Never edit or commit","kind":"design"},{"path":"vendor/cordis/src/context.ts","line":42,"expect":"export class Context {","kind":"source"},{"path":"vendor/cordis/src/fiber.ts","line":184,"expect":"export class Fiber {","kind":"source"},{"path":"vendor/cordis/src/registry.ts","line":316,"expect":"plugin(plugin: Plugin","kind":"source"},{"path":"vendor/cordis/src/events.ts","line":32,"expect":"DispatchMode","kind":"source"},{"path":"vendor/cordis/src/service.ts","line":11,"expect":"export abstract class Service","kind":"source"},{"path":"apps/cli/src/bin.ts","line":30,"expect":"case 'profile'","kind":"source"},{"path":"apps/cli/src/profile-boot.ts","line":207,"expect":"export async function runProfile","kind":"source"},{"path":"packages/boot/app-boot/src/profile.ts","line":36,"expect":"PROFILES_DIR","kind":"source"},{"path":"packages/core/agent-loop/src/agent.ts","line":230,"expect":"systemPrompt.assemble","kind":"source"},{"path":"packages/core/agent-loop/src/agent.ts","line":234,"expect":"dispatch.waterfall","kind":"source"},{"path":"packages/core/agent-loop/src/agent.ts","line":255,"expect":"turn/start","kind":"source"},{"path":"packages/core/agent-loop/src/agent.ts","line":279,"expect":"step/start","kind":"source"},{"path":"packages/core/agent-loop/src/agent.ts","line":292,"expect":"step/end","kind":"source"},{"path":"packages/core/agent-loop/src/agent.ts","line":319,"expect":"turn/end","kind":"source"},{"path":"packages/core/agent-loop/src/agent.ts","line":341,"expect":"deriveMessages()","kind":"source"},{"path":"packages/core/agent-loop/src/agent.ts","line":345,"expect":"llm.stream(request)","kind":"source"},{"path":"packages/core/agent-loop/src/agent.ts","line":349,"expect":"assistant/chunk","kind":"source"},{"path":"packages/core/agent-loop/src/agent.ts","line":382,"expect":"assistant/message","kind":"source"},{"path":"packages/core/agent-loop/src/agent.ts","line":395,"expect":"executeToolCalls","kind":"source"},{"path":"packages/core/agent-loop/src/tool-calls.ts","line":263,"expect":"tool/call","kind":"source"},{"path":"packages/core/agent-loop/src/tool-calls.ts","line":281,"expect":"tool/result","kind":"source"},{"path":"packages/core/tools/src/index.ts","line":1342,"expect":"async execute(exec: ToolExecutionInput)","kind":"source"},{"path":"packages/core/tools/src/index.ts","line":1476,"expect":"tools/pre-execute","kind":"source"},{"path":"packages/core/tools/src/index.ts","line":1574,"expect":"tools/execute","kind":"source"},{"path":"packages/core/tools/src/index.ts","line":1744,"expect":"tools/post-execute","kind":"source"},{"path":"packages/core/session/src/index.ts","line":425,"expect":"export class Session {","kind":"source"},{"path":"packages/core/session/src/index.ts","line":604,"expect":"append<T extends SessionEventType>","kind":"source"},{"path":"packages/core/session/src/index.ts","line":726,"expect":"deriveMessages(): Message[]","kind":"source"},{"path":"packages/core/session/src/index.ts","line":792,"expect":"export class SessionStore extends Service","kind":"source"},{"path":"packages/core/session/src/types.ts","line":56,"expect":"SESSION_FORMAT_VERSION","kind":"source"},{"path":"packages/session/session-persistence/src/coordinator.ts","line":1123,"expect":"session/event","kind":"source"},{"path":"packages/session/session-persistence-sqlite/src/schema.ts","line":20,"expect":"SCHEMA_VERSION = 15","kind":"source"},{"path":"packages/shell/shell/src/index.ts","line":40,"expect":"declare module '@deepseek-ai/cordis'","kind":"source"},{"path":"packages/core/agent-loop/tests/loop.spec.ts","line":198,"expect":"round-trips tool calls","kind":"test"},{"path":"packages/core/agent-loop/tests/tool-calls.spec.ts","line":99,"expect":"tool-call scheduler","kind":"test"},{"path":"packages/core/session/tests/session.spec.ts","line":903,"expect":"deep-freezes","kind":"test"}]},"headers":[],"relativePath":"harness-explained.md","filePath":"harness-explained.md"}`),u={name:"harness-explained.md"};function h(x,e,k,m,b,E){const r=p("Mermaid");return c(),g("div",null,[e[4]||(e[4]=d('<h1 id="一页理解-harness" tabindex="-1">一页理解 Harness <a class="header-anchor" href="#一页理解-harness" aria-label="Permalink to &quot;一页理解 Harness&quot;">​</a></h1><h2 id="这页解决什么问题" tabindex="-1">这页解决什么问题 <a class="header-anchor" href="#这页解决什么问题" aria-label="Permalink to &quot;这页解决什么问题&quot;">​</a></h2><p>你打开 DeepSeek Harness 仓库，看到 <code>packages/</code> 下 49 个分组目录、219 个 package、<code>vendor/</code> 里另一个完整框架、<code>docs/</code> 里几十篇英文文档。你想知道的其实只有三件事：<strong>它是什么？它由哪几块组成？一个请求进来后怎么走完？</strong></p><p>这页就回答这三件事，不多不少。读完你应该能对着仓库指出五六个关键文件，并说出它们各自负责什么。深入某个子系统请走 <a href="/one-turn">/one-turn</a>、<a href="/session-log">/session-log</a>、<a href="/capability-seam">/capability-seam</a>、<a href="/cordis-lifecycle">/cordis-lifecycle</a>；找具体源码位置请走 <a href="/repo-map">/repo-map</a> 和 <a href="/source-order">/source-order</a>。</p><h2 id="先用人话解释" tabindex="-1">先用人话解释 <a class="header-anchor" href="#先用人话解释" aria-label="Permalink to &quot;先用人话解释&quot;">​</a></h2><p><strong>Harness（马具 / 承载框架）就是&quot;包在大模型外面、让它能真的干活的那一整套东西&quot;。</strong></p><p>模型本身只会做一件事：给它一段文字，它吐回一段文字。它不会读文件、不会跑命令、不会记住上次说过什么、不会在你按下 Ctrl-C 时停手。这些全部是 Harness 的活。</p><p>具体说，DeepSeek Harness 负责这些事：</p><ul><li>把散落的&quot;上下文&quot;（系统提示、可用工具的描述、历史对话、当前目录信息）拼成一次模型请求。</li><li>接住模型流式吐回的内容，把它拆成&quot;文本块&quot;和&quot;工具调用请求&quot;。</li><li>真的去执行那些工具调用——跑 shell、读写文件、搜网页、开子 Agent——并把结果送回给模型。</li><li>判断&quot;这一轮结束了吗&quot;：模型还想继续调工具就再来一步，没事干了就收尾。</li><li>把上面每一个事实<strong>按顺序写进一本只能追加的账本</strong>，这样你重启、分叉、回放、导出都还原得出来。</li></ul><p>DeepSeek Harness 的特别之处，是它把上面每一块都做成了<strong>插件</strong>。项目根目录的 <code>AGENTS.md:3</code> 第一句就写着这是 &quot;a plugin-based agent harness on vendored Cordis: <strong>everything is a plugin</strong>&quot;，并且明确要求改 <code>packages/</code> 之前先读 <code>docs/architecture.md</code>。【设计记录】</p><p>&quot;一切皆插件&quot;不是口号。官方架构文档 <code>docs/architecture.md:11</code> 说得更直白：模型适配器、工具注册表、会话日志、<strong>乃至 agent loop 本身</strong>都是插件，所以每一块都能从配置里换掉；<code>:13</code> 补一句&quot;没有一个特权核心可以打补丁&quot;——你扩展 dsh 的方式是<strong>在其他插件旁边再挂一个插件</strong>。【设计记录】</p><p>支撑这件事的框架叫 <strong>Cordis</strong>，它不在 <code>node_modules</code> 里，而是被 vendored（内联）到本仓库的 <code>vendor/cordis</code>，作为 pnpm workspace 成员一起构建。</p><h2 id="智能工厂比喻" tabindex="-1">智能工厂比喻 <a class="header-anchor" href="#智能工厂比喻" aria-label="Permalink to &quot;智能工厂比喻&quot;">​</a></h2><p>把 DeepSeek Harness 想成一座<strong>智能工厂</strong>。</p><p><strong>厂区公共设施与连接系统</strong>就是 Cordis 的 <code>Context</code>（<code>vendor/cordis/src/context.ts:42</code> 的 <code>export class Context {</code>）。水电气、传送带、广播系统都在这儿。任何设备接上它就能用到全厂的资源，也能对全厂广播。</p><p><strong>可插拔工作站</strong>就是 plugin。每个工作站有自己的接口规格，推到厂区里插上就开始工作，拔掉就干净地消失——它铺过的临时管线、订阅过的广播、占用过的编号全部回收。负责记住&quot;这台工作站到底占了什么、拔掉时要还什么&quot;的账本叫 <strong>Fiber</strong>（<code>vendor/cordis/src/fiber.ts:184</code> 的 <code>export class Fiber {</code>）。注意：一台工作站型号可以在厂里插好几台，每插一次产生一个 Fiber，所以 Fiber 是&quot;工作站的一次运行实例&quot;，不是型号本身。</p><p><strong>工厂方案和设备套装</strong>就是 profile 和 bundle。<code>docs/architecture.md:17</code> 说一个运行中的 <code>dsh</code> 是&quot;a plugin tree composed at boot from ordered layers&quot;（启动时由有序层级组装出的插件树）。<strong>profile</strong> 是存在 Harness home 里的一份具名组装方案（<code>:19</code>），列出它要叠哪些 bundle；<strong>bundle</strong> 是配置行加代码的分发格式（<code>:21</code>），像一箱预配好的设备套装。<code>web</code> 和 <code>headless</code> 是随仓库发布的两个方案模板。【设计记录】</p><p><strong>统一能力接口</strong>就是 Service Definition，<strong>真正提供能力的机器</strong>就是 Provider，<strong>使用能力的岗位</strong>就是 Consumer。比如&quot;执行 shell 命令&quot;这个能力：接口写在 <code>packages/shell/shell</code>，本机执行的机器是 <code>packages/shell/bash-local</code>，沙箱里执行的机器是 <code>packages/shell/bash-sandbox</code>，而模型能看见并调用的那个岗位是 <code>packages/shell/tool-bash</code>。换机器不换岗位，岗位完全感觉不到。</p><p><strong>任务工长</strong>就是 Agent。它拿到一个任务，决定&quot;现在该问模型什么、模型说要用哪台机器、结果拿到后接着干还是收工&quot;。工长的调度流程就是 <strong>Agent loop</strong>，实现在 <code>packages/core/agent-loop</code>。</p><p><strong>只能追加的生产流水账</strong>就是 Session log。工长每做一件事就往流水账上追加一行，<strong>只能追加，不能改、不能删</strong>。账上的每一行叫一条 <strong>Session event</strong>（一条生产事实）。整个工厂的&quot;记忆&quot;就是这本账，模型每次看到的历史都是从这本账现算出来的。</p><h2 id="比喻的限制" tabindex="-1">比喻的限制 <a class="header-anchor" href="#比喻的限制" aria-label="Permalink to &quot;比喻的限制&quot;">​</a></h2><p>工厂比喻能让你进门，但它在四个地方会明确失真。这段专门点明，之后本页回到真实术语。</p><p><strong>第一，并发不像流水线。</strong> 工厂比喻暗示工序一个接一个。真实的工具调用是有调度器的：模型一次可以要求调多个工具，<code>packages/core/agent-loop/src/tool-calls.ts</code> 里的调度器会判断哪些能并行、哪些必须串行、哪里要设屏障（barrier）。测试文件 <code>packages/core/agent-loop/tests/tool-calls.spec.ts:99</code> 的 <code>describe(&#39;tool-call scheduler: grouping and barriers&#39;, ...)</code> 第一条用例就叫 &quot;runs parallel-safe siblings concurrently (all start before any completes)&quot;——它断言并行安全的兄弟调用<strong>全部开始之后才有任何一个完成</strong>。【测试行为】所以别把工具执行想成一条单线传送带。</p><p><strong>第二，&quot;广播&quot;其实分五种模式，别混。</strong> <code>vendor/cordis/src/events.ts:32</code> 定义 <code>export type DispatchMode = &#39;emit&#39; | &#39;parallel&#39; | &#39;serial&#39; | &#39;bail&#39; | &#39;waterfall&#39;</code>。【源码事实】其中两种最容易搞错：<strong>waterfall</strong>（瀑布）的监听者拿到值可以改写再往下传，<strong>必须调用 <code>next()</code> 才能把控制权交给下一个</strong>；<strong>serial</strong>（串行）只是按顺序跑完，<strong>没有 <code>next()</code></strong>。<code>docs/architecture.md:84</code> 明确列出哪些是 waterfall：<code>agent/pre-step</code>、<code>agent/request</code>、<code>llm/stream</code> 和三个 <code>tools/*</code>；而 <code>agent/turn-stopping</code> 是 serial。【设计记录】工厂里&quot;喊一声大家都听见&quot;的直觉在这里不够用——waterfall 更像一份<strong>依次盖章、每个人都能改内容的会签单</strong>，漏盖一个章（不调 <code>next()</code>）流程就断在那儿。</p><p><strong>第三，生命周期不是&quot;装上就一直在&quot;。</strong> 真实工厂里设备装了就长期在那。Cordis 的插件可以随时卸载，而且卸载必须<strong>可逆</strong>：它注册的服务、事件监听、定时器都是&quot;效果&quot;（effect），随 Fiber 一起回收。这套机制是 Cordis 最需要单独学的部分，见 <a href="/cordis-lifecycle">/cordis-lifecycle</a>。</p><p><strong>第四，&quot;账本&quot;和&quot;现场状态&quot;是两回事（live state vs durable state）。</strong> 流水账上的一行（Session event）是<strong>持久事实</strong>，重启后还在，会通过 <code>session/event</code> 广播出去。但工长手上正在处理的那个活对象（live <code>Agent</code>，通过 <code>agent/*</code> 事件传递：inbox、step、status、request 等）只存在于内存，<strong>它不进账本</strong>。<code>docs/architecture.md:57-58</code> 把这两类事件分得很清：需要活过重载的事实用 session event，观察或拦截&quot;正在进行中的工作&quot;用 agent event。【设计记录】把这两个混起来是新读者最常见的错。</p><p>以下段落全部使用真实术语。比喻和真实术语的完整对照见 <a href="/glossary">/glossary</a>。</p><h2 id="真实概念和结构图" tabindex="-1">真实概念和结构图 <a class="header-anchor" href="#真实概念和结构图" aria-label="Permalink to &quot;真实概念和结构图&quot;">​</a></h2>',28)),(c(),n(i,null,{default:t(()=>[a(r,{id:"mermaid-108",class:"mermaid",graph:"flowchart%20TD%0A%20%20P%5B%22profile%20%2F%20bundle%3Cbr%2F%3E%E9%85%8D%E7%BD%AE%E5%B1%82%22%5D%20--%3E%20L%5B%22Cordis%20Context%3Cbr%2F%3E%E6%8F%92%E4%BB%B6%E6%A0%91%22%5D%0A%20%20L%20--%3E%20AG%5B%22core%2Fagent-loop%3Cbr%2F%3E%E5%9B%9E%E5%90%88%E9%A9%B1%E5%8A%A8%22%5D%0A%20%20L%20--%3E%20SS%5B%22core%2Fsession%3Cbr%2F%3E%E5%8F%AA%E8%BF%BD%E5%8A%A0%E6%97%A5%E5%BF%97%22%5D%0A%20%20L%20--%3E%20T%5B%22core%2Ftools%3Cbr%2F%3E%E5%B7%A5%E5%85%B7%E6%B3%A8%E5%86%8C%E4%B8%8E%E7%AE%A1%E9%81%93%22%5D%0A%20%20L%20--%3E%20LM%5B%22llm%2Fllm%3Cbr%2F%3E%E6%A8%A1%E5%9E%8B%E6%8E%A5%E7%BC%9D%22%5D%0A%20%20AG%20--%3E%20SS%0A%20%20AG%20--%3E%20T%0A%20%20AG%20--%3E%20LM%0A%20%20T%20--%3E%20SEAM%5B%22%E5%90%84%E8%83%BD%E5%8A%9B%E6%8E%A5%E7%BC%9D%3Cbr%2F%3Eshell%20%2F%20fs%20%2F%20web%20...%22%5D%0A%20%20SS%20--%3E%20PER%5B%22session-persistence-sqlite%3Cbr%2F%3E%E8%90%BD%E7%9B%98%22%5D%0A"})]),fallback:t(()=>[...e[0]||(e[0]=[o(" Loading... ",-1)])]),_:1})),e[5]||(e[5]=s("p",null,[o("图说明：最上层是 profile 与 bundle 组成的配置层，它在启动时把插件树装配进 Cordis Context。Context 之下并列挂着四个核心插件："),s("code",null,"core/agent-loop"),o("（回合驱动）、"),s("code",null,"core/session"),o("（只追加日志）、"),s("code",null,"core/tools"),o("（工具注册表与执行管道）、"),s("code",null,"llm/llm"),o("（模型接缝）。agent-loop 同时依赖另外三者：向 session 追加事实、向 tools 派发调用、向 llm 发请求。tools 之下是各个具体能力接缝（shell、fs、web 等），session 之下是持久化插件负责落盘。")],-1)),s("details",null,[e[2]||(e[2]=s("summary",null,"完整版（节点较多，可放大查看）",-1)),(c(),n(i,null,{default:t(()=>[a(r,{id:"mermaid-114",class:"mermaid",graph:"flowchart%20TD%0A%20%20BIN%5B%22bin.ts%22%5D%20--%3E%20PB%5B%22profile-boot.ts%22%5D%0A%20%20PB%20--%3E%20AB%5B%22boot%2Fapp-boot%22%5D%0A%20%20AB%20--%3E%20BD%5B%22bundle%2Fbase%20%E7%AD%89%22%5D%0A%20%20BD%20--%3E%20REG%5B%22registry.plugin%22%5D%0A%20%20REG%20--%3E%20FIB%5B%22Fiber%22%5D%0A%20%20FIB%20--%3E%20CTX%5B%22Cordis%20Context%22%5D%0A%20%20CTX%20--%3E%20AGENT%5B%22core%2Fagent%22%5D%0A%20%20CTX%20--%3E%20LOOP%5B%22core%2Fagent-loop%22%5D%0A%20%20CTX%20--%3E%20SESS%5B%22core%2Fsession%22%5D%0A%20%20CTX%20--%3E%20TOOLS%5B%22core%2Ftools%22%5D%0A%20%20CTX%20--%3E%20SP%5B%22core%2Fsystem-prompt%22%5D%0A%20%20CTX%20--%3E%20LLM%5B%22llm%2Fllm%22%5D%0A%20%20LLM%20--%3E%20DS%5B%22llm-deepseek%20%E7%AD%89%22%5D%0A%20%20TOOLS%20--%3E%20TB%5B%2221%20%E4%B8%AA%20tool-*%22%5D%0A%20%20TB%20--%3E%20SHELL%5B%22shell%2Fshell%22%5D%0A%20%20SHELL%20--%3E%20BL%5B%22bash-local%22%5D%0A%20%20SHELL%20--%3E%20BS%5B%22bash-sandbox%22%5D%0A%20%20SESS%20--%3E%20COORD%5B%22session-persistence%22%5D%0A%20%20COORD%20--%3E%20SQL%5B%22sqlite%20%E5%90%8E%E7%AB%AF%22%5D%0A%20%20COORD%20--%3E%20JSONL%5B%22jsonl%20%E5%90%8E%E7%AB%AF%22%5D%0A%20%20CTX%20--%3E%20HOST%5B%22host%2Fwebserver%22%5D%0A"})]),fallback:t(()=>[...e[1]||(e[1]=[o(" Loading... ",-1)])]),_:1})),e[3]||(e[3]=d("<p>完整版图说明：这张图把启动链路和运行期插件树画在一起，节点标签为了不撑破正文栏都用了短名，完整路径见下。左上从 <code>apps/cli/src/bin.ts</code>（<code>dsh</code> 命令入口）开始，经 <code>apps/cli/src/profile-boot.ts</code> 与 <code>packages/boot/app-boot</code> 解析出 profile，展开成 <code>bundle/base</code>、<code>bundle/web-app</code>、<code>bundle/headless</code> 等 bundle 层（图里的 <code>bundle/base 等</code>）；每一个配置行通过 cordis 的 <code>registry.plugin()</code> 挂载，每次挂载产生<strong>一个</strong> Fiber，所有 Fiber 共享一棵 Context 树。Context 上并列注册着 <code>core/agent</code>、<code>core/agent-loop</code>、<code>core/session</code>、<code>core/tools</code>、<code>core/system-prompt</code>、<code>llm/llm</code> 六个核心插件。<code>llm/llm</code> 之下挂具体模型 Provider（如 <code>llm-deepseek</code>）；<code>core/tools</code> 之下挂 21 个 <code>tool-*</code> Consumer 包，其中 <code>tool-bash</code> 透过 <code>shell/shell</code> 接口打到 <code>bash-local</code> 与 <code>bash-sandbox</code> 两个并列 Provider（图里画成两个分支）；<code>core/session</code> 之下由 <code>session-persistence</code> 协调器分发到 SQLite 后端（<code>session-persistence-sqlite</code>，其 <code>SCHEMA_VERSION</code> 为 15）或 JSONL 后端（<code>session-persistence-jsonl</code>）；Context 上还挂着 <code>host/webserver</code> 以及它带起来的 <code>client/*</code> 前端插件族。</p>",1))]),e[6]||(e[6]=d('<p>现在逐个说清图里的方块，全部给出源码位置。</p><p><strong>Cordis Context</strong> —— <code>vendor/cordis/src/context.ts:42</code>，<code>export class Context {</code>。每个插件运行在自己的 Context 里，Context 从父级继承服务与事件通道。插件挂载入口是 <code>vendor/cordis/src/registry.ts:316</code> 的 <code>plugin(plugin: Plugin, config?: any, ...)</code>。服务的基类是 <code>vendor/cordis/src/service.ts:11</code> 的 <code>export abstract class Service&lt;out T = never&gt; {</code>。【源码事实】</p><p><strong>Fiber</strong> —— <code>vendor/cordis/src/fiber.ts:184</code>，<code>export class Fiber {</code>。这个文件 754 行，是 Cordis 里最值得完整读一遍的单文件。类注释自己说明了职责：&quot;tracks dependency state, validated config, lifecycle effects, and cleanup for the plugin context returned by <code>ctx.plugin()</code>&quot;。【源码事实】</p><p><strong>core/session</strong> —— <code>packages/core/session/src/index.ts:425</code> 的 <code>export class Session {</code>，追加入口是同文件 <code>:604</code> 的 <code>append&lt;T extends SessionEventType&gt;(</code>，投影出模型历史的方法是 <code>:726</code> 的 <code>deriveMessages(): Message[] {</code>，服务本体是 <code>:792</code> 的 <code>export class SessionStore extends Service {</code>。日志的事件词表版本号是 <code>packages/core/session/src/types.ts:56</code> 的 <code>export const SESSION_FORMAT_VERSION = 0</code>。【源码事实】这个日志是<strong>不可变的</strong>：<code>packages/core/session/tests/session.spec.ts:903</code> 有一条用例 <code>it(&#39;deep-freezes seeded and appended event snapshots&#39;, ...)</code>，断言追加进去的事件快照被深冻结。【测试行为】</p><p><strong>core/agent-loop</strong> —— <code>packages/core/agent-loop/src/agent.ts</code>，496 行，是整个产品的心脏。四个回合边界事件都在这个文件里追加：<code>:255</code> <code>turn/start</code>、<code>:279</code> <code>step/start</code>、<code>:292</code> <code>step/end</code>、<code>:319</code> <code>turn/end</code>。【源码事实】</p><p><strong>core/tools</strong> —— <code>packages/core/tools/src/index.ts</code>，执行入口是 <code>:1342</code> 的 <code>async execute(exec: ToolExecutionInput): Promise&lt;ToolExecutionResult&gt; {</code>，三个可扩展的 waterfall 点在 <code>:1476</code>（<code>tools/pre-execute</code>）、<code>:1574</code>（<code>tools/execute</code>）与 <code>:1744</code> 附近（<code>tools/post-execute</code>）。【源码事实】</p><p><strong>turn 与 step 的定义</strong> —— <code>docs/architecture.md:65</code> 是权威表述：一个 <strong>step</strong> 是&quot;一次模型请求加上它调的那些工具&quot;；一个 <strong>turn</strong> 是零个或多个 step，&quot;在它的第一份输入被领走之前打开，在没有任何欠账时关闭&quot;。注意 turn 可以有<strong>零个</strong> step——被 <code>agent/pre-step</code> 拒绝的输入依然会开一个 turn 再关掉，好让日志留下这次尝试的痕迹。【设计记录】</p><p><strong>能力接缝（capability seam）</strong> —— <code>docs/architecture.md:100</code> 定义为&quot;a swappable capability with three roles&quot;：Service Definition 声明接口、Service Provider 实现它、Consumer 使用它（通常是模型可见的工具）。<code>AGENTS.md:109</code> 把这条写成了硬规则：接缝<strong>必须三个角色齐全</strong>，单独一个角色不叫接缝。<code>AGENTS.md:111</code> 进一步指定 <code>dsh-shell</code> 的 request/spec 拆分是这类设计的<strong>模板</strong>。【设计记录】<code>packages/shell/shell/src/index.ts:40</code> 的 <code>declare module &#39;@deepseek-ai/cordis&#39;</code> 块就是接口如何往 Context 上声明 <code>ctx.shell</code> 的范例。【源码事实】</p><p><strong>&quot;模型看得见就必须记下来&quot;</strong> —— <code>docs/architecture.md:96</code> 的 <strong>Model-visible means logged</strong>：任何进入模型请求的东西都必须能从日志重建，而且有运行时不变量在断言这件事。所以加一种新的模型可见输入，必然要加一种新的 session event。【设计记录】</p><h2 id="一条真实调用路径" tabindex="-1">一条真实调用路径 <a class="header-anchor" href="#一条真实调用路径" aria-label="Permalink to &quot;一条真实调用路径&quot;">​</a></h2><p>下面是&quot;你在终端敲一句话，Harness 跑一个工具再答复你&quot;的真实文件链路，<strong>22 跳</strong>，每跳都是实测过的路径和行号。</p><ol><li><code>apps/cli/src/bin.ts:30</code> —— <code>case &#39;profile&#39;: {</code>，动态 import <code>./profile-boot.ts</code>。这个文件顶部注释说明&quot;Dynamic imports per mode keep unrelated modes out of each dispatch path&quot;。【源码事实】</li><li><code>apps/cli/src/profile-boot.ts:207</code> —— <code>export async function runProfile(options: RunProfileOptions): Promise&lt;{ ctx: Context; shutdown: ProcessShutdown }&gt;</code>，返回值就是一棵活的 Context 加一个关机器。【源码事实】</li><li><code>packages/boot/app-boot/src/profile.ts:36</code> —— <code>export const PROFILES_DIR = &#39;profiles&#39;</code>，profile 目录在 Harness home 之下；同文件把每个 bundle 的 patch 层按 <code>dsh.profile.bundles</code> 顺序叠到一个空的 entry 列表上。【源码事实】</li><li><code>vendor/cordis/src/registry.ts:316</code> —— <code>plugin(plugin: Plugin, config?: any, getOuterStack = buildOuterStack()) {</code>，装配出来的每一个配置行都从这里挂进插件树。【源码事实】</li><li><code>vendor/cordis/src/fiber.ts:184</code> —— <code>export class Fiber {</code>，每次 <code>ctx.plugin()</code> 产生一个 Fiber，记录这次挂载的依赖状态与需要回收的副作用。【源码事实】</li><li><code>packages/core/agent-loop/src/agent.ts:255</code> —— <code>this.session.append(&#39;turn/start&#39;, { turn })</code>。回合正式开门，这是第一条落进日志的事实。【源码事实】</li><li><code>packages/core/agent-loop/src/agent.ts:230</code> —— <code>await this.loopCtx.systemPrompt.assemble(...)</code>，把各插件注册的提示段落和工具 schema 装配起来。【源码事实】</li><li><code>packages/core/agent-loop/src/agent.ts:234</code> —— <code>await this.dispatch.waterfall(&#39;agent/pre-step&#39;, ...)</code>。<strong>这是&quot;模型将看到什么&quot;的最后一道会签</strong>：监听者可以改写领到的消息，也可以直接 reject。默认回调返回 <code>{ kind: &#39;enter&#39;, messages: ... }</code>。【源码事实】</li><li><code>packages/core/agent-loop/src/agent.ts:279</code> —— <code>this.session.append(&#39;step/start&#39;, { turn, step })</code>。这一步才真的要花一次模型调用。【源码事实】</li><li><code>packages/core/agent-loop/src/agent.ts:341</code> —— <code>this.session.deriveMessages()</code> 作为参数传进 <code>buildRequest</code>。<strong>模型历史不是某个变量里存的数组，是从日志现算出来的投影。</strong> 【源码事实】</li><li><code>packages/core/agent-loop/src/agent.ts:345</code> —— <code>const stream = preparedCall?.stream(request) ?? this.loopCtx.llm.stream(request)</code>，请求真的发出去了，返回一个异步流。【源码事实】</li><li><code>packages/core/agent-loop/src/agent.ts:349</code> —— <code>chunkSeqs.push(this.session.append(&#39;assistant/chunk&#39;, { turn, step, chunk }).seq)</code>。<strong>每一个流式分片都被单独记账</strong>，这就是回放和 UI 保真的来源。【源码事实】</li><li><code>packages/core/agent-loop/src/agent.ts:382</code> —— 追加 <code>&#39;assistant/message&#39;</code>，并用 <code>sourceEventSeqs: chunkSeqs</code> 把这条完整消息与它的分片来源绑起来。【源码事实】</li><li><code>packages/core/agent-loop/src/agent.ts:395</code> —— <code>const { concluded } = await executeToolCalls(this.loopCtx, turn, step, toolCalls, signal, ...)</code>。模型要求调工具了，交给调度器。【源码事实】</li><li><code>packages/core/agent-loop/src/tool-calls.ts:263</code> —— <code>const event = session.append(&#39;tool/call&#39;, { turn, step, callId: block.id, name: block.name, arguments: block.arguments })</code>。调用意图先记账，再执行。【源码事实】</li><li><code>packages/core/tools/src/index.ts:1342</code> —— <code>async execute(exec: ToolExecutionInput): Promise&lt;ToolExecutionResult&gt; {</code>，进入工具执行管道。【源码事实】</li><li><code>packages/core/tools/src/index.ts:1476</code> —— <code>&#39;tools/pre-execute&#39;</code> waterfall。权限审批、危险命令拦截都挂在这里。【源码事实】</li><li><code>packages/core/tools/src/index.ts:1574</code> —— <code>&#39;tools/execute&#39;</code> waterfall。超时策略等&quot;包一层&quot;的插件在这层，比如 <code>packages/guard/timeout-policy</code>。【源码事实】</li><li>真正干活的 Provider 被调用。以 Bash 为例，Consumer 是 <code>packages/shell/tool-bash</code>，接口在 <code>packages/shell/shell/src/index.ts:40</code> 声明的 <code>ctx.shell</code>，Provider 是 <code>packages/shell/bash-local</code>（本机）或 <code>packages/shell/bash-sandbox</code>（沙箱）。【跨源码分析】</li><li><code>packages/core/agent-loop/src/tool-calls.ts:281</code> —— <code>session.append(&#39;tool/result&#39;, {...})</code>，结果记账，并引用第 15 跳那条 <code>tool/call</code> 的 seq。【源码事实】</li><li><code>packages/core/agent-loop/src/agent.ts:292</code> 与 <code>:319</code> —— <code>step/end</code> 与 <code>turn/end</code>。<code>:319</code> 在 <code>finally</code> 块里，注释写着 &quot;every exit assigns a turn ending&quot;：<strong>无论正常完成、出错还是被取消，turn 一定会被关上。</strong> 【源码事实】</li><li><code>packages/session/session-persistence/src/coordinator.ts:1123</code> —— <code>ctx.on(&#39;session/event&#39;, (session, event) =&gt; {</code>，持久化协调器订阅每一条事实，攒批后写进后端；SQLite 后端的磁盘结构版本是 <code>packages/session/session-persistence-sqlite/src/schema.ts:20</code> 的 <code>export const SCHEMA_VERSION = 15</code>。【源码事实】</li></ol><p>想看这条链路的最小可执行版本？读 <code>packages/core/agent-loop/tests/loop.spec.ts:198</code> 的 <code>it(&#39;round-trips tool calls: model requests tool → executes → result in next request&#39;, ...)</code>——它用一个 mock adapter 走完了第 6 到 20 跳。【测试行为】</p><p>逐跳讲解见 <a href="/one-turn">/one-turn</a>。</p><h2 id="源码与测试锚点" tabindex="-1">源码与测试锚点 <a class="header-anchor" href="#源码与测试锚点" aria-label="Permalink to &quot;源码与测试锚点&quot;">​</a></h2><p>如果你只想抄一份&quot;最小必读清单&quot;，就是这张表。</p><table tabindex="0"><thead><tr><th>你想搞清楚</th><th>读这里</th><th>关键行</th></tr></thead><tbody><tr><td>插件树怎么装起来</td><td><code>apps/cli/src/bin.ts</code>、<code>apps/cli/src/profile-boot.ts</code></td><td><code>bin.ts:30</code>、<code>profile-boot.ts:207</code></td></tr><tr><td>profile 与 bundle 的层叠规则</td><td><code>packages/boot/app-boot/src/profile.ts</code></td><td><code>:36</code> <code>PROFILES_DIR</code></td></tr><tr><td>Context 是什么</td><td><code>vendor/cordis/src/context.ts</code>（146 行）</td><td><code>:42</code> <code>export class Context {</code></td></tr><tr><td>插件卸载为什么干净</td><td><code>vendor/cordis/src/fiber.ts</code>（754 行）</td><td><code>:184</code> <code>export class Fiber {</code></td></tr><tr><td>事件有几种派发模式</td><td><code>vendor/cordis/src/events.ts</code></td><td><code>:32</code> <code>DispatchMode</code></td></tr><tr><td>日志怎么追加、怎么投影</td><td><code>packages/core/session/src/index.ts</code></td><td><code>:425</code>、<code>:604</code>、<code>:726</code>、<code>:792</code></td></tr><tr><td>日志版本号</td><td><code>packages/core/session/src/types.ts</code></td><td><code>:56</code> <code>SESSION_FORMAT_VERSION = 0</code></td></tr><tr><td>回合边界</td><td><code>packages/core/agent-loop/src/agent.ts</code></td><td><code>:255</code>、<code>:279</code>、<code>:292</code>、<code>:319</code></td></tr><tr><td>模型请求怎么发</td><td>同上</td><td><code>:341</code>、<code>:345</code>、<code>:349</code>、<code>:382</code></td></tr><tr><td>工具怎么调度与并发</td><td><code>packages/core/agent-loop/src/tool-calls.ts</code></td><td><code>:263</code>、<code>:281</code></td></tr><tr><td>工具执行管道的拦截点</td><td><code>packages/core/tools/src/index.ts</code></td><td><code>:1342</code>、<code>:1476</code>、<code>:1574</code></td></tr><tr><td>接缝怎么声明服务</td><td><code>packages/shell/shell/src/index.ts</code></td><td><code>:40</code> <code>declare module</code></td></tr><tr><td>落盘的磁盘结构版本</td><td><code>packages/session/session-persistence-sqlite/src/schema.ts</code></td><td><code>:20</code> <code>SCHEMA_VERSION = 15</code></td></tr><tr><td>事实怎么流到持久化</td><td><code>packages/session/session-persistence/src/coordinator.ts</code></td><td><code>:1123</code> <code>session/event</code></td></tr></tbody></table><p>测试锚点（当文档用，比读实现快）：</p><ul><li><code>packages/core/agent-loop/tests/loop.spec.ts:198</code> —— 工具调用往返的完整最小例。【测试行为】</li><li><code>packages/core/agent-loop/tests/tool-calls.spec.ts:99</code> —— 调度器的分组与屏障语义。【测试行为】</li><li><code>packages/core/session/tests/session.spec.ts:903</code> —— 事件快照被深冻结。【测试行为】</li></ul><p>设计记录锚点（&quot;为什么这么做&quot;只在这类文件里）：</p><ul><li><code>AGENTS.md:3</code>、<code>:109</code>、<code>:111</code> —— 一切皆插件；接缝必须三角齐全；<code>dsh-shell</code> 是模板。【设计记录】</li><li><code>docs/architecture.md:11</code>、<code>:17</code>、<code>:19</code>、<code>:21</code>、<code>:65</code>、<code>:84</code>、<code>:96</code>、<code>:100</code> —— 全站引用最密的 8 行。【设计记录】</li></ul><p>顺便说清本站与官方文档的边界：正文必须留在 <code>docs/</code>，<code>website/</code> 只放 VitePress 配置与发布清单（<code>website/AGENTS.md:7</code>：<code>website/</code> &quot;owns only VitePress configuration, presentation assets, and the publication manifest&quot;），生成物目录 <code>.generated/</code> 不可编辑（同文件 <code>:11</code>）。本站在 <code>learn/</code>，与这两者都不重叠。【设计记录】</p><h2 id="常见误解" tabindex="-1">常见误解 <a class="header-anchor" href="#常见误解" aria-label="Permalink to &quot;常见误解&quot;">​</a></h2><p><strong>误解一：&quot;agent loop 是核心，其他都是外围插件。&quot;</strong> 不是。<code>docs/architecture.md:11</code> 明确把 agent loop 本身列为可替换插件，<code>:13</code> 说&quot;没有特权核心&quot;。<code>packages/core/agent</code>（8 个包的 core 组之一）定义的是 <code>Agent</code> <strong>接口</strong>，<code>packages/core/agent-loop</code> 只是<strong>默认驱动</strong>。你可以挂一个别的驱动实现同一接口。【设计记录】</p><p><strong>误解二：&quot;session log 就是聊天记录。&quot;</strong> 不是。它是<strong>产生模型上下文的唯一来源</strong>，聊天记录只是它的一种投影。<code>deriveMessages()</code>（<code>packages/core/session/src/index.ts:726</code>）是投影函数，同一份日志还能投影出转录、遥测、UI 时间线。而且它记的粒度比聊天记录细得多——连每个流式分片（<code>assistant/chunk</code>）都单独一行。【源码事实】</p><p><strong>误解三：&quot;改一个工具就是改那个工具的包。&quot;</strong> 大概率不够。一个能力是三角（<code>AGENTS.md:109</code>），改行为要先判断你动的是接口、实现还是消费方。改接口会同时影响所有 Provider 和 Consumer。【设计记录】</p><p><strong>误解四：&quot;<code>vendor/cordis</code> 是第三方依赖，不用管。&quot;</strong> 它是 pnpm workspace 成员，和 <code>packages/*/*</code> 一起构建，同步流程记在 <code>vendor/README.md</code>。你可以不改它，但读它是必要的——不懂 Fiber 就看不懂&quot;卸载为什么干净&quot;。见 <a href="/cordis-lifecycle">/cordis-lifecycle</a>。</p><p><strong>误解五：&quot;219 个 package 是过度工程。&quot;</strong> 换个角度看：21 个 <code>tool-*</code> 包和 32 个 <code>ui-*</code> 包（实测数量）占了近四分之一，它们本质是&quot;一个能力一个包&quot;的直接结果。<code>docs/architecture.md:102</code> 给出的收益是：文件系统和子进程 Provider 共享同一个执行世界，把它们指向远程沙箱，Bash、PTY、LSP <strong>一起搬走，不需要 fork 任何 Provider</strong>。分包的代价换来的是这个。【设计记录】</p><p><strong>误解六：&quot;waterfall 事件里我不调 <code>next()</code> 只是少做一步。&quot;</strong> 不是少做一步，是<strong>流程断在你这儿</strong>。<code>docs/architecture.md:84</code> 说 waterfall 的监听者&quot;must call <code>next()</code> to delegate&quot;。而 serial 的 <code>agent/turn-stopping</code> 根本没有 <code>next()</code>，两者写法不能互抄。【设计记录】</p><h2 id="三道自测题" tabindex="-1">三道自测题 <a class="header-anchor" href="#三道自测题" aria-label="Permalink to &quot;三道自测题&quot;">​</a></h2><p><strong>第一题</strong>：某人给 Harness 加了一个功能：每次请求前把当前 git 分支名塞进系统提示。他在 <code>agent/pre-step</code> 的监听里读 <code>git branch</code> 然后拼到消息里，没有新增任何 session event。这个实现违反了什么原则？会在什么场景下暴露出问题？</p><details><summary>参考答案</summary><p>违反 <code>docs/architecture.md:96</code> 的 <strong>Model-visible means logged</strong>：任何进入模型请求的内容都必须能从日志重建，而且有运行时不变量在断言这件事。</p><p>暴露场景至少两类。第一，<strong>回放/续跑不一致</strong>：模型历史是 <code>deriveMessages()</code>（<code>packages/core/session/src/index.ts:726</code>）从日志现算的，日志里没有这段分支名，重启后重建出的上下文就和当时发出去的请求不一样了。第二，<strong>不变量直接报错</strong>：<code>docs/architecture.md:96</code> 说有运行时断言，<code>packages/core/agent-loop/src/invariant.ts</code> 与 <code>packages/core/session/src/invariant.ts</code> 都存在，<code>packages/core/agent-loop/tests/invariant.spec.ts</code> 专测这类失败。</p><p>正确做法：扩展 <code>SessionEventMap</code> 加一种事件，从日志渲染进请求。参照 <code>packages/context/agent-instructions</code>、<code>packages/context/time-context</code> 这几个&quot;往上下文里塞东西&quot;的包是怎么做的。</p></details><p><strong>第二题</strong>：你在 <code>tools/execute</code> 上挂了一个插件，想给所有工具加统一超时。同事在 <code>agent/turn-stopping</code> 上挂了一个插件，想在回合收尾时写一份总结。两人都照抄了对方的写法。谁会出问题，为什么？</p><details><summary>参考答案</summary><p><strong>照抄的方向决定谁挂。</strong> <code>docs/architecture.md:84</code>：<code>tools/execute</code> 是 <strong>waterfall</strong>，监听者必须调 <code>next()</code>；<code>agent/turn-stopping</code> 是 <strong>serial</strong>，没有 <code>next()</code>。</p><ul><li>如果超时插件抄了 serial 写法（不调 <code>next()</code>），<code>tools/execute</code> 的会签链在它这里断掉，<strong>后续包装器和真正的派发都不会跑</strong>，工具直接不执行。这是硬故障。</li><li>如果总结插件抄了 waterfall 写法，去调一个不存在的 <code>next()</code>，会直接抛错；就算被容错吞掉，它也拿不到 waterfall 那种&quot;改写并往下传&quot;的语义，因为 serial 不传值。</li></ul><p>深层教训：<code>vendor/cordis/src/events.ts:32</code> 的五种 <code>DispatchMode</code> 决定了监听者的<strong>签名契约</strong>，挂事件前先确认这个事件是哪种模式，不能凭事件名猜。</p></details><p><strong>第三题</strong>：<code>SESSION_FORMAT_VERSION</code> 现在是 <code>0</code>（<code>packages/core/session/src/types.ts:56</code>），SQLite 的 <code>SCHEMA_VERSION</code> 已经是 <code>15</code>（<code>packages/session/session-persistence-sqlite/src/schema.ts:20</code>）。一个已经迭代到 15 的项目，为什么另一个版本号还停在 0？这两个数字各自版本化的是什么？</p><details><summary>参考答案</summary><p>它们版本化的是<strong>两个正交的东西</strong>，源码注释自己说了。</p><p><code>SCHEMA_VERSION</code>（<code>schema.ts</code> 的注释）版本化的是<strong>磁盘表结构</strong>，&quot;Bumped only on a breaking change to the table layout; orthogonal to a session&#39;s own <code>version</code>&quot;。表结构改一次就 +1，改了 15 次，很正常。</p><p><code>SESSION_FORMAT_VERSION</code>（<code>types.ts</code> 的注释）版本化的是<strong>事件词表</strong>（&quot;the EVENT vocabulary&quot;），并且<strong>按 session 存在 <code>sessions</code> 行里</strong>，不是全局的。</p><p>停在 0 的原因是显式的策略选择，不是忘了：<code>AGENTS.md:7</code>（Pre-release stance 段）写着后端拒绝旧的磁盘格式，SQLite 用单调递增的 <code>SCHEMA_VERSION</code>，而 <code>dsh-session</code> 把 <code>SESSION_FORMAT_VERSION</code> <strong>保持在 <code>0</code> 且不做任何兼容承诺</strong>。发布前没有外部消费者，所以选&quot;正确的地基&quot;而不是兼容垫片。</p><p>推论：这两个数字<strong>不能互相替代</strong>。换持久化后端（SQLite → JSONL）不动事件词表；加一种新 session event 类型才可能需要动 <code>SESSION_FORMAT_VERSION</code>。</p></details><h2 id="下一步阅读建议" tabindex="-1">下一步阅读建议 <a class="header-anchor" href="#下一步阅读建议" aria-label="Permalink to &quot;下一步阅读建议&quot;">​</a></h2><ul><li>想按顺序把架构读透（推荐）→ <a href="/cordis-lifecycle">/cordis-lifecycle</a> → <a href="/one-turn">/one-turn</a> → <a href="/session-log">/session-log</a> → <a href="/capability-seam">/capability-seam</a></li><li>想先摸清仓库版图、知道每个分组干什么 → <a href="/repo-map">/repo-map</a></li><li>已经有具体任务、要挑源码精读 → <a href="/source-order">/source-order</a></li><li>术语对不上 → <a href="/glossary">/glossary</a></li><li>想知道本页哪些结论只是推断 → <a href="/evidence-guide">/evidence-guide</a> 与 <a href="/coverage">/coverage</a></li></ul>',38))])}const f=l(u,[["render",h]]);export{S as __pageData,f as default};
